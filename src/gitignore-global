# Agent Coordinator — prevent generated artifacts from being committed

# Handoff artifacts
handoff_active.md
handoff_*.md
context_summary_*.md

# Swarm artifacts
swarm-manifest.md
swarm_status.json

# Consultation artifacts
consult_request.md
consult_response.md

# Swarm working documents
# Note: spec.md and plan.md are NOT excluded globally to avoid suppressing
# user files. Projects using /swarm should add them to a project-level .gitignore
# or use `git add -f` for source templates.

# Runtime artifacts (may contain PII)
quota_snapshot.json

# Antigravity configs
.antigravity-configs/
